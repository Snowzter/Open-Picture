#install pip if u havn't
#pip install Pillow (in the terminal)

from PIL import Image

im = Image.open(r"location of the picture")


# Remove the line under me if u don't want to open it multiple times.
for x in range(how many times u want to repeat it):
    im.show()
